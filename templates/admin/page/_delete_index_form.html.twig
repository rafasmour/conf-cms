<form method="post" action="{{ path('app_admin_page_delete', {'id': page.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ page.id) }}">
    <button class="text-blue-500 hover:text-blue-700 ml-2">Delete</button>
</form>
