{% extends '/admin/base.admin.html.twig' %}

{% block title %}{{ page.title }}{% endblock %}

{% block body %}
    <h1 class="p-3 font-bold text-2xl">Edit Page</h1>
    <div class="w-80 mx-10">
        {{ include('admin/page/_form.html.twig', {'button_label': 'Update'}) }}
    </div>

    <a href="{{ path('app_admin_page_index') }}">back to list</a>

    {{ include('admin/page/_delete_form.html.twig') }}
{% endblock %}
